# Media assets

Put images, video, audio, and other webpage assets here. Reference them with paths such as `media/logo.svg` from your HTML or CSS.

Large binary media should use Git LFS before it is committed.
